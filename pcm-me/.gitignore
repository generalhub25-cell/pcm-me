node_modules
.next
next-env.d.ts
.env
.env*
*.db
*.db-shm
*.db-wal
*.db.bak-*
/media
# Payload Images-collection upload dir (default staticDir = ./images).
# Local/staging uploaded + migration-downloaded media — never commit (use cloud
# storage in production). The tracked images/verify.png stays tracked.
/images/
build
dist
.DS_Store
*.log

.vercel

# Content-migration scratch + crawled artifacts (Stage A dry-run)
migration-work/
